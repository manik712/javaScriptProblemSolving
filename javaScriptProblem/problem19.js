/*

Create a function that finds the
maximum range of a triangle's 
third edge, where the side lengths
are all integers.


*/

function thirdEdge(s1, s2) {
  const s3 = s1 + s2 - 1;
  return s3
}

console.log(thirdEdge(2,5));
//6
